<UserControl x:Class="TaskManagementSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100"
             Loaded="OnLoaded">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20,18"/>
        </Style>

        <Style x:Key="CardCaptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#64748B"/>
        </Style>

        <Style x:Key="CardNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
        </Style>

        <Style x:Key="CardFootnoteStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
        </Style>

        <Style x:Key="StatusBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Foreground" Value="#4F6BED"/>
            <Setter Property="Background" Value="#EEF2F7"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F8FAFC">
        <Grid Margin="32,24,32,32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Dashboard" FontSize="26" FontWeight="Bold" Foreground="#0F172A"/>
                <TextBlock Text="A quick view of the work across every task."
                           Foreground="#64748B" FontSize="13" Margin="0,6,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Total tasks" Style="{StaticResource CardCaptionStyle}"/>
                        <TextBlock Text="{Binding TotalTasks}" Style="{StaticResource CardNumberStyle}"/>
                        <TextBlock Text="Every task on record" Style="{StaticResource CardFootnoteStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="In progress" Style="{StaticResource CardCaptionStyle}"/>
                        <TextBlock Text="{Binding InProgressCount}" Style="{StaticResource CardNumberStyle}"/>
                        <TextBlock Text="Currently being worked on" Style="{StaticResource CardFootnoteStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Completed" Style="{StaticResource CardCaptionStyle}"/>
                        <TextBlock Text="{Binding CompletedCount}" Style="{StaticResource CardNumberStyle}"/>
                        <TextBlock Text="Closed tasks" Style="{StaticResource CardFootnoteStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="6" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Due this week" Style="{StaticResource CardCaptionStyle}"/>
                        <TextBlock Text="{Binding DueThisWeekCount}" Style="{StaticResource CardNumberStyle}"
                                   Foreground="#B45309"/>
                        <TextBlock Text="Needs attention" Style="{StaticResource CardFootnoteStyle}"
                                   Foreground="#B45309"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Tasks by status" FontSize="17" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Current distribution" Style="{StaticResource CardFootnoteStyle}" Margin="0,4,0,18"/>

                    <ItemsControl ItemsSource="{Binding StatusBreakdown}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Status}" FontSize="13"
                                               Foreground="#334155" VerticalAlignment="Center"/>

                                    <ProgressBar Grid.Column="1" Style="{StaticResource StatusBarStyle}"
                                                 Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}"
                                                 Value="{Binding Count, Mode=OneWay}"
                                                 Maximum="{Binding DataContext.TotalTasks,
                                                           RelativeSource={RelativeSource AncestorType=ItemsControl},
                                                           Mode=OneWay}"
                                                 VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="2" Text="{Binding Count}" FontSize="13" FontWeight="Bold"
                                               Foreground="#0F172A" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Upcoming deadlines" FontSize="17" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="{Binding UpcomingDaysLabel}" Style="{StaticResource CardFootnoteStyle}" Margin="0,4,0,8"/>

                    <ItemsControl ItemsSource="{Binding UpcomingDeadlines}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="0,14">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding RequiredBy, StringFormat='{}{0:dd}'}"
                                                       FontSize="19" FontWeight="Bold" Foreground="#0F172A"/>
                                            <TextBlock Text="{Binding RequiredBy, StringFormat='{}{0:MMM}'}"
                                                       FontSize="11" Foreground="#94A3B8"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Description}" FontSize="13" FontWeight="SemiBold"
                                                       Foreground="#0F172A" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding AssignedToName}" FontSize="12"
                                                       Foreground="#94A3B8" Margin="0,3,0,0"/>
                                        </StackPanel>

                                        <Border Grid.Column="2" CornerRadius="10" Padding="10,3" VerticalAlignment="Center"
                                                Background="{Binding Status, Converter={StaticResource StatusToBrush}, ConverterParameter=Background}">
                                            <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="Bold"
                                                       Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Nothing due in this window." Margin="0,14,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource CardFootnoteStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding UpcomingDeadlines.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>

            <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="#B91C1C" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </ScrollViewer>
</UserControl>
