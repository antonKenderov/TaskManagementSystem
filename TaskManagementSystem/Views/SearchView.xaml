<UserControl x:Class="TaskManagementSystem.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TaskManagementSystem.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             Loaded="OnLoaded">
    <UserControl.Resources>
        <local:EnumInitialsConverter x:Key="EnumInitials"/>
    </UserControl.Resources>

    <Grid Background="#F8FAFC" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="32,24,32,0">
            <TextBlock Text="Search comments" FontSize="26" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="Search every comment on every task and open the task it belongs to."
                       Foreground="#64748B" FontSize="13" Margin="0,6,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="32,20,32,0" Background="White" BorderBrush="#93B4F5"
                BorderThickness="1" CornerRadius="6" Padding="12,10" HorizontalAlignment="Left" Width="700">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Path Grid.Column="0" Width="15" Height="15" Stretch="Uniform" Fill="#94A3B8"
                      VerticalAlignment="Center" Margin="0,0,10,0"
                      Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z"/>

                <Grid Grid.Column="1">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                             Background="Transparent" BorderThickness="0" FontSize="14"
                             Foreground="#0F172A" VerticalAlignment="Center"/>
                    <TextBlock Text="Search comments by keyword..." Foreground="#94A3B8" FontSize="14"
                               VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SearchText}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SearchText}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="32,28,32,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="{Binding Results.Count, StringFormat={}{0} results}"
                           FontWeight="SemiBold" FontSize="13" Foreground="#334155"/>
                <TextBlock Text="most recent" FontSize="13" Foreground="#94A3B8" Margin="8,0,0,0"
                           Visibility="{Binding ShowingRecent, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>

            <TextBlock Grid.Column="1" Text="Double-click a result to open its task"
                       FontSize="13" Foreground="#94A3B8"/>
        </Grid>

        <ScrollViewer Grid.Row="3" Margin="32,8,32,24" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="Transparent" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                                Padding="0,18" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                              Command="{Binding DataContext.OpenTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="30" Height="30" CornerRadius="15"
                                        Background="#E4EAFB" VerticalAlignment="Top" Margin="0,0,14,0">
                                    <TextBlock Text="{Binding Type, Converter={StaticResource EnumInitials}}"
                                               FontSize="11" FontWeight="Bold" Foreground="#2955CA"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Type}" FontWeight="Bold" FontSize="13" Foreground="#0F172A"/>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                                   FontSize="12" Foreground="#94A3B8" Margin="10,1,0,0"/>
                                        <TextBlock Text="{Binding ReminderDate, StringFormat='{}reminder {0:yyyy-MM-dd}'}"
                                                   FontSize="12" Foreground="#B45309" Margin="10,1,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ReminderDate}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <TextBlock Text="{Binding Text}" FontSize="13" Foreground="#334155"
                                               TextWrapping="Wrap" Margin="0,6,0,0"/>

                                    <TextBlock Text="{Binding TaskDescription}" FontSize="12" Foreground="#2563EB"
                                               TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>
                                </StackPanel>

                                <Path Grid.Column="2" Width="10" Height="10" Stretch="Uniform" Fill="#94A3B8"
                                      VerticalAlignment="Center" Margin="16,0,0,0"
                                      Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="#B91C1C" FontSize="13"
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
